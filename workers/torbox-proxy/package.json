{
  "name": "torbot-dl-proxy",
  "private": true,
  "version": "1.0.0",
  "description": "Free Cloudflare Worker proxy for TorBot (WebDAV / API / CDN)",
  "main": "worker.js",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
